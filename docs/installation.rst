.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install heppi

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv heppi
    $ pip install heppi
